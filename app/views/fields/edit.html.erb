<div class="page-header">
  <%= link_to magazine_issue_fields_path(@magazine, @issue), :class => 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to magazine_issue_field_path(@magazine, @issue, @field), :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-info-sign"></span>
    Show
  <% end %>
  <h1>Editing field</h1>
</div>

<%= render 'form' %>
