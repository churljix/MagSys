<div class="page-header">
  <%= link_to magazine_issues_path(@magazine), :class => 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to magazine_issue_path(@magazine, @issue), :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-info-sign"></span>
    Show
  <% end %>
  <h1>Editing issue</h1>
</div>

<%= render 'form' %>
